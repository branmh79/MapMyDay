/*
@Dao
public interface EventDao {
    @Insert
    void insert(Event event);

    @Query("SELECT * FROM events WHERE date BETWEEN :startDate AND :endDate")
    List<Event> getEventsBetweenDates(String startDate, String endDate);
}
*/